<!-- Selected state is passed by a click event on elements with the 'selectable' class -->
<!-- Counts calculated from TodoList -->             
<table>
    <thead>
        <tr class="category" id="{{id}}">
            <td><img src="assets/{{iconPath}}"></img></td>
            <td><a class="selectable" href="#"><h2>{{title}}</h2></a></td>
            <td><span>{{todosCount}}</span></td>
        </tr>
    </thead>
    <tbody class="subcategories">
        <!-- generated by template -->
    </tbody>
</table>
